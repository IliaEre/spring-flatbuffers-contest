phantom:
  address: localhost:9999
  ammo_type: phantom
  ammofile: ammo.txt
  load_profile:
    load_type: rps
    schedule: line(100, 400, 3m) # starting from 1rps growing linearly to 10rps during 10 minutes
  writelog: all
  ssl: false
  connection_test: true
autostop:
  autostop:
    - http(5xx,100%,30s)
    - net(xx,1,30)